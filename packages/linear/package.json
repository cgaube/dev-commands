{
  "name": "@devcommands/linear",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "linear": "bun run src/main.ts",
    "build": "bun run ts:check && bun build src/main.ts --outfile dist/linear",
    "compile": "bun run ts:check && bun build src/main.ts --compress --compile --outdir bin --outfile bin/linear",
    "compile:watch": "bun run compile --watch",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "colorize-template": "^1.0.0",
    "commander": "^14.0.1",
    "execa": "^9.6.0",
    "picocolors": "^1.1.1"
  }
}
