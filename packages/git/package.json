{
  "name": "@devcommands/git",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "git": "bun run src/main.ts",
    "build": "bun run check && bun build src/main.ts --outfile dist/test",
    "compile": "bun run check && bun build src/main.ts --compress --compile --outfile bin/test",
    "compile:watch": "bun run compile --watch",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "colorize-template": "^1.0.0",
    "commander": "^14.0.1",
    "execa": "^9.6.0",
    "picocolors": "^1.1.1"
  }
}
