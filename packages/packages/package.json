{
  "name": "@devcommands/packages",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run ts:check && bun build src/main.ts --outfile dist/packages",
    "compile": "bun run ts:check && bun build src/main.ts --compress --compile --outfile bin/packages",
    "compile:watch": "bun run compile --watch",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0",
    "colorize-template": "^1.0.0",
    "commander": "^15.0.0",
    "execa": "^9.6.1",
    "handlebars": "^4.7.9"
  }
}
