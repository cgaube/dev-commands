{
  "name": "@devcommands/jira",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "jira": "bun run src/main.ts",
    "compile": "bun run ts:check && bun build src/main.ts --compress --compile --outfile bin/jira",
    "compile:watch": "bun run compile --watch",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.8.1",
    "@types/voca": "^1.4.6",
    "colorize-template": "^1.0.0",
    "commander": "^15.0.0",
    "execa": "^10.0.1",
    "jira.js": "^5.4.0",
    "picocolors": "^1.1.1",
    "voca": "^1.4.1"
  }
}
