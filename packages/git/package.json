{
  "name": "@devcommands/git",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run ts:check && bun build src/main.ts --outfile dist/git",
    "compile": "bun run ts:check && bun build src/main.ts --compress --compile --outfile bin/git",
    "compile:watch": "bun run compile --watch",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "colorize-template": "^1.0.0",
    "commander": "^14.0.3",
    "execa": "^9.6.1",
    "picocolors": "^1.1.1"
  }
}
